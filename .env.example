# Vite environment variables for KisanShakti AI
# Copy to .env.local (for local dev), .env.staging, .env.production and fill values

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: default language/tenant for local dev only
VITE_DEFAULT_LANGUAGE=hi
VITE_DEFAULT_TENANT_SLUG=default

